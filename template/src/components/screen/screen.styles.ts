import { styled } from '@theme/theme';

export const ScreenContent = styled('View', {
  flex: 1,
});

export const ScreenScrollView = styled('ScrollView', {
  flex: 1,
  position: 'relative',
});

export const ScreenContentInner = styled('View', {
  flex: 1,
  position: 'relative',
});
