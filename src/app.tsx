import React from 'react';
import { SafeAreaView } from 'react-native';
import AppComposer from './app.composer';

const App = () => {
  return (
    <SafeAreaView>
      <AppComposer />
    </SafeAreaView>
  );
};

export default App;
