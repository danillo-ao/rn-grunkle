import { styled } from '@theme/theme';

export const HomeScreenInner = styled('View', {
  flex: 1,
  alignItems: 'center',
  justifyContent: 'center',
});

export const HomeScreenImage = styled('Image', {
  width: 150,
  height: 150,
});
